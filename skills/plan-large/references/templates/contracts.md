# Contracts (new or changed APIs)

(Only include this file when the feature exposes new or changed APIs.
Delete the file if there are no contract changes — empty contracts.md
is worse than no contracts.md.)

## <Endpoint / RPC / event name>

- **Path / method:** `GET /api/v1/widgets`
- **Request shape:** `{ … }`
- **Response shape:** `{ … }`
- **Error codes:** 400 (…), 404 (…), 409 (…)
- **Auth:** <session / API key / scope required>
- **Rate limit:** <quota, if any>
- **Backwards compat:** <new endpoint / additive change / breaking — and migration window>

## <next contract>

…
